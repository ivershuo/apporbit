name: Collect JP and KR ranking probes

on:
  schedule:
    - cron: "17 4 * * *"
      timezone: "Asia/Tokyo"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    uses: ./.github/workflows/collect.yml
    with:
      market: JP,KR
