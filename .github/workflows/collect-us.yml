name: Collect US ranking probes

on:
  schedule:
    - cron: "17 4 * * *"
      timezone: "America/New_York"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    uses: ./.github/workflows/collect.yml
    with:
      market: US
