name: Collect DE ranking probes

on:
  schedule:
    - cron: "17 4 * * *"
      timezone: "Europe/Berlin"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    uses: ./.github/workflows/collect.yml
    with:
      market: DE
