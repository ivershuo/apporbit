name: Collect GB ranking probes

on:
  schedule:
    - cron: "17 4 * * *"
      timezone: "Europe/London"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    uses: ./.github/workflows/collect.yml
    with:
      market: GB
