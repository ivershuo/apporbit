name: Collect BR ranking probes

on:
  schedule:
    - cron: "17 4 * * *"
      timezone: "America/Sao_Paulo"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect:
    uses: ./.github/workflows/collect.yml
    with:
      market: BR
